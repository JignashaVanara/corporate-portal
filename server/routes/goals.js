var router = require('express').Router();
var connection  = require('../models/db');

//add comments for required goals of performace check
router.get('/addcomments', function(req, res, next) {
})

module.exports = router;