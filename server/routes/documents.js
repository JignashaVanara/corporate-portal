var router = require('express').Router();
var connection  = require('../models/db');

//view documents like offer letter, payslips etc.
router.get('/viewdocs', function(req, res, next) {
})

module.exports = router;